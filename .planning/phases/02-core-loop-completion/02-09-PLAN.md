---
phase: 02-core-loop-completion
plan: 09
type: execute
wave: 5
depends_on: ["02-01", "02-02", "02-03", "02-04", "02-05", "02-07", "02-08"]
files_modified:
  - backend/tests/conftest.py
  - backend/tests/test_integration_loop.py
  - backend/tests/test_quota_enforcement.py
  - frontend/src/tests/integration/chat.spec.ts
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "Integration test runs full loop: upload → summary → quiz → attempt → weak areas → practice"
    - "Quota enforcement tests verify free/pro limits work"
    - "Frontend chat integration test sends message and receives stream"
    - "Tests pass in CI environment"
  artifacts:
    - path: "backend/tests/conftest.py"
      provides: "Test fixtures (db, client, authenticated user)"
      contains: "pytest.fixture"
    - path: "backend/tests/test_integration_loop.py"
      provides: "Full loop integration test"
      contains: "def test_full_learning_loop"
    - path: "backend/tests/test_quota_enforcement.py"
      provides: "Quota limit tests per feature"
      contains: "def test_quota_exceeded"
    - path: "frontend/src/tests/integration/chat.spec.ts"
      provides: "Playwright test for chat UI"
      contains: "test('chat flow')"
  key_links:
    - from: "backend/tests/test_integration_loop.py"
      to: "backend/src/api/routers/"
      via: "FastAPI TestClient calls"
      pattern: "TestClient"
    - from: "backend/tests/test_quota_enforcement.py"
      to: "backend/src/services/quota_service.py"
      via: "Mock datetime for reset testing"
      pattern: "freeze_time"
    - from: "frontend/src/tests/integration/chat.spec.ts"
      to: "frontend/src/app/(app)/chat/page.tsx"
      via: "page.goto('/chat')"
      pattern: "page.goto"
---

<objective>
Create integration tests for the complete learning loop and quota enforcement.

Purpose: Verify the entire system works end‑to‑end as specified in D‑23. Ensure quota limits are enforced, frontend chat UI functions, and all components integrate correctly.

Output: pytest suite for backend integration, Playwright test for chat UI, tests pass locally and in CI.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-loop-completion/02-CONTEXT.md
@docs/dev/architecture.md
@docs/dev/testing-strategy.md
@backend/src/api/routers/
@frontend/src/app/(app)/chat/page.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: Set up pytest fixtures and test database</name>
  <files>backend/tests/conftest.py</files>
  <read_first>
    - docs/dev/testing-strategy.md (testing approach)
    - backend/src/api/routers/summaries.py (example route)
    - backend/src/db/models.py (models)
    - alembic.ini (database config)
  </read_first>
  <acceptance_criteria>
    - `conftest.py` provides `db` fixture (AsyncSession)
    - `client` fixture returns authenticated TestClient
    - `test_user` fixture creates a user with Clerk‑like JWT
    - Database is cleaned after each test (rollback)
  </acceptance_criteria>
  <action>
1. Create `backend/tests/` directory if not exists.
2. Write `backend/tests/conftest.py`:
   - Import `pytest`, `asyncio`, `FastAPI`, `TestClient`.
   - Create `event_loop` fixture for async tests.
   - Create `db` fixture that yields an AsyncSession and rolls back after test.
   - Create `app` fixture that returns the FastAPI app (import from `main`).
   - Create `client` fixture that returns `TestClient(app)`.
   - Create `test_user` fixture that generates a mock JWT with `sub` claim and adds to `client` headers.
   - Use `pytest_asyncio` for async support.

3. Configure test database:
   - Use SQLite in‑memory database for tests.
   - Run Alembic migrations on test setup.
   - Ensure test isolation (each test gets clean DB).

4. Verify: `pytest backend/tests/ --collect-only` runs without errors.
  </action>
  <verify>
    <automated>pytest backend/tests/ --collect-only</automated>
  </verify>
  <done>Test fixtures provide db, client, and authenticated user.</done>
</task>

<task type="auto" tdd="true">
  <name>task 2: Write full learning loop integration test</name>
  <files>backend/tests/test_integration_loop.py</files>
  <read_first>
    - .planning/phases/02-core-loop-completion/02-CONTEXT.md (D‑23 description)
    - backend/src/api/routers/documents.py (upload endpoint)
    - backend/src/api/routers/summaries.py (summary endpoint)
    - backend/src/api/routers/quizzes.py (quiz endpoints)
    - backend/src/api/routers/practice.py (practice endpoints)
  </read_first>
  <acceptance_criteria>
    - Test follows sequence: upload doc → request summary → generate quiz → submit attempt → get weak areas → create practice
    - Each step validates expected response structure
    - Test passes with real chain calls (may use mocked Gemini if needed)
    - Database state verified after each step
  </acceptance_criteria>
  <action>
1. Create `backend/tests/test_integration_loop.py`:
   - Import fixtures from `conftest`.
   - Write `test_full_learning_loop(client, db, test_user)`.
   - Steps:
     a. Upload a sample PDF (use test file from `backend/tests/data/sample.pdf` or mock).
     b. Poll job status until completed.
     c. Request summary for document.
     d. Create quiz from document.
     e. Submit quiz attempt with answers.
     f. Fetch weak areas (`GET /practice/weak-areas`).
     g. Create practice drill for weak topics.
     h. Submit practice attempt.

2. Mock Gemini calls if needed (use `unittest.mock.patch` on `google.generativeai.GenerativeModel`).
   - Return deterministic responses that match expected shape.

3. Assert each response status code and data shape.
4. Verify database records created (Quiz, QuizAttempt, UserTopicPerformance, PracticeSession).

5. Run test: `pytest backend/tests/test_integration_loop.py -v`.
  </action>
  <verify>
    <automated>pytest backend/tests/test_integration_loop.py -v</automated>
  </verify>
  <done>Full loop test passes, covering all AI features.</done>
</task>

<task type="auto" tdd="true">
  <name>task 3: Test quota enforcement for all features</name>
  <files>backend/tests/test_quota_enforcement.py</files>
  <read_first>
    - backend/src/services/quota_service.py (TIER_LIMITS, reset logic)
    - backend/src/api/deps.py (enforce_quota)
    - docs/dev/architecture.md (quota as route dependency)
  </read_first>
  <acceptance_criteria>
    - Free user hits quota limit after N calls (per feature)
    - Pro user has higher limits
    - Quota resets at midnight PH time (mock datetime)
    - Error response when quota exceeded is 429 with clear message
  </acceptance_criteria>
  <action>
1. Create `backend/tests/test_quota_enforcement.py`:
   - Import `freezegun` or `time_machine` for datetime mocking.
   - Write tests:
     - `test_free_user_quota_limits`: Call each feature (summary, quiz, practice, chat) up to limit, verify 429 after limit.
     - `test_pro_user_higher_limits`: Similar with pro tier limits.
     - `test_quota_reset_at_midnight`: Set time to 23:59, use quota, advance to 00:01, verify quota reset.
     - `test_quota_dependency_injects_correctly`: Verify `enforce_quota` appears in route decorators.

2. Mock user tier: Modify `test_user` fixture to set `tier="free"` or `tier="pro"`.

3. Use `pytest.mark.parametrize` to test all four features.

4. Ensure tests don't rely on real Gemini calls (mock chains).

5. Run: `pytest backend/tests/test_quota_enforcement.py -v`.
  </action>
  <verify>
    <automated>pytest backend/tests/test_quota_enforcement.py -v</automated>
  </verify>
  <done>Quota limits enforced correctly; reset logic works.</done>
</task>

<task type="auto" tdd="true">
  <name>task 4: Write frontend chat integration test (Playwright)</name>
  <files>frontend/src/tests/integration/chat.spec.ts</files>
  <read_first>
    - frontend/src/app/(app)/chat/page.tsx (page structure)
    - frontend/src/components/features/chat/ (component APIs)
    - frontend/playwright.config.ts (if exists)
    - docs/dev/testing-strategy.md (frontend testing approach)
  </read_first>
  <acceptance_criteria>
    - Test navigates to chat page
    - Selects a document, types message, clicks send
    - Verifies streaming animation appears with "Searching your notes..."
    - Verifies response appears with citations
  </acceptance_criteria>
  <action>
1. Set up Playwright if not already:
   - `pnpm add -D @playwright/test`
   - Create `playwright.config.ts` with baseURL `http://localhost:3000`.

2. Create `frontend/src/tests/integration/chat.spec.ts`:
   - Import `test`, `expect` from `@playwright/test`.
   - Write `test('chat flow', async ({ page }) => { ... })`.
   - Steps:
     a. Mock API responses (use `page.route()` to intercept calls to `/documents`, `/chat`).
     b. Navigate to `/chat`.
     c. Wait for document list to load.
     d. Check a document checkbox.
     e. Fill message input.
     f. Click send.
     g. Assert that loading animation appears with text "Searching your notes...".
     h. Mock SSE stream response with citations.
     i. Assert response appears in message thread with citation badges.

3. Run test: `pnpm exec playwright test chat.spec.ts`.

4. Ensure test runs in CI mode (headless).
  </action>
  <verify>
    <automated>pnpm exec playwright test --project=chromium frontend/src/tests/integration/chat.spec.ts</automated>
  </verify>
  <done>Chat UI integration test passes, covering waiting animation and citation display.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test → Production DB | Test database isolation |
| Mock → Real service | Gemini API mocking |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-09-01 | Spoofing | Test authentication | mitigate | Use separate test JWT secret, not production |
| T-02-09-02 | Tampering | Test data | accept | Test data is ephemeral |
| T-02-09-03 | Repudiation | Test execution | accept | Not applicable |
| T-02-09-04 | Information Disclosure | Test DB contents | accept | In‑memory SQLite, no PII |
| T-02-09-05 | Denial of Service | Test suite | accept | Tests run in CI with resource limits |
| T-02-09-06 | Elevation of Privilege | Test fixtures | mitigate | Ensure test users cannot access real production data |
</threat_model>

<verification>
- Run `pytest backend/tests/` – all tests pass.
- Run `pnpm exec playwright test` – chat integration test passes.
- Run `ruff check backend/tests/` – no style violations.
</verification>

<success_criteria>
- Full learning loop integration test passes (D‑23).
- Quota enforcement tests pass for all features.
- Frontend chat integration test passes.
- Test suite can run in CI (GitHub Actions).
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-loop-completion/02-09-SUMMARY.md`
</output>