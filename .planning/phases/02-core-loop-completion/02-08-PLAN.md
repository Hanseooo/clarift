---
phase: 02-core-loop-completion
plan: 08
type: execute
wave: 4
depends_on: ["02-04", "02-06"]
files_modified:
  - frontend/src/app/(app)/chat/page.tsx
  - frontend/src/components/features/chat/chat-input.tsx
  - frontend/src/components/features/chat/chat-messages.tsx
  - frontend/src/components/features/chat/document-selector.tsx
  - frontend/src/hooks/use-chat.ts
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "User can send chat messages with document selection"
    - "Chat responses stream via SSE with three-dot pulse animation and 'Searching your notes...' label"
    - "Citations display shows which document chunks were used"
    - "UI follows design tokens and uses appropriate error/empty states"
  artifacts:
    - path: "frontend/src/app/(app)/chat/page.tsx"
      provides: "Chat page (Server Component)"
      contains: "export default function ChatPage"
    - path: "frontend/src/components/features/chat/chat-input.tsx"
      provides: "Message input with document selector"
      exports: ["ChatInput"]
    - path: "frontend/src/components/features/chat/chat-messages.tsx"
      provides: "Streaming message display with citations"
      exports: ["ChatMessages"]
    - path: "frontend/src/hooks/use-chat.ts"
      provides: "React Query hooks for chat API calls"
      exports: ["useSendChatMessage"]
  key_links:
    - from: "frontend/src/app/(app)/chat/page.tsx"
      to: "frontend/src/lib/api-client.ts"
      via: "API calls to fetch documents for selector"
      pattern: "client.GET.*documents"
    - from: "frontend/src/components/features/chat/chat-input.tsx"
      to: "frontend/src/hooks/use-chat.ts"
      via: "useSendChatMessage hook"
      pattern: "useSendChatMessage"
    - from: "frontend/src/components/features/chat/chat-messages.tsx"
      to: "frontend/src/lib/api-types.ts"
      via: "ChatMessage type import"
      pattern: "ChatMessage"
---

<objective>
Build frontend chat UI with streaming responses, document selection, and citation display.

Purpose: Complete the chat feature end-to-end. Users need a way to ask questions about their uploaded documents and see AI responses with citations. Covers D-26 (Chat UI) and D-29 (waiting state animation).

Output: Functional chat page with message input, document selector, streaming responses, citation display, and proper loading/error states.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-loop-completion/02-CONTEXT.md
@docs/dev/architecture.md
@docs/dev/master-spec.md
@docs/dev/design.md
@frontend/src/app/(app)/documents/page.tsx
@frontend/src/components/features/summary/
@backend/src/api/routers/chat.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: Create chat page structure and document selector</name>
  <files>frontend/src/app/(app)/chat/page.tsx, frontend/src/components/features/chat/document-selector.tsx</files>
  <read_first>
    - frontend/src/app/(app)/documents/page.tsx (Server Component pattern)
    - frontend/src/components/features/summary/ (feature component structure)
    - docs/dev/design.md (design tokens)
    - backend/src/api/routers/chat.py (endpoint spec)
  </read_first>
  <acceptance_criteria>
    - Chat page exists at `/chat` route
    - Page fetches user's documents for selector (Server Component)
    - Document selector component allows choosing which documents to chat with
    - UI follows design tokens from design.md
  </acceptance_criteria>
  <action>
1. Create `frontend/src/app/(app)/chat/page.tsx`:
   - Use Server Component pattern (no "use client").
   - Fetch documents via API client (`client.GET("/documents")`).
   - Render a layout with header, document selector, and message area.
   - Import and use `DocumentSelector` component.

2. Create `frontend/src/components/features/chat/document-selector.tsx`:
   - Accept `documents` prop (array of {id, title}).
   - Allow multi‑select with checkboxes.
   - Display selected count.
   - Follow design tokens (colors, spacing).

3. Ensure page is accessible via navigation (add link later if needed).

4. Verify: `pnpm run build` passes (no TypeScript errors).
  </action>
  <verify>
    <automated>pnpm run build</automated>
  </verify>
  <done>Chat page renders with document selector; documents load from backend.</done>
</task>

<task type="auto" tdd="true">
  <name>task 2: Implement chat input component with message sending</name>
  <files>frontend/src/components/features/chat/chat-input.tsx, frontend/src/hooks/use-chat.ts</files>
  <read_first>
    - frontend/src/lib/api-client.ts (API client methods)
    - frontend/src/lib/api-types.ts (generated types)
    - docs/dev/design.md (input component styling)
    - backend/src/api/routers/chat.py (POST /chat request shape)
  </read_first>
  <acceptance_criteria>
    - Input component has textarea and send button
    - Hook `useSendChatMessage` calls POST /chat with {message, document_ids}
    - Hook handles loading state and errors
    - Selected document IDs are passed to API
  </acceptance_criteria>
  <action>
1. Create `frontend/src/hooks/use-chat.ts`:
   - Implement `useSendChatMessage` using React Query's `useMutation`.
   - Mutation should POST to `/chat` with `{message: string, document_ids: string[]}`.
   - Return `mutateAsync`, `isLoading`, `error`.

2. Create `frontend/src/components/features/chat/chat-input.tsx`:
   - Accept `selectedDocumentIds` prop and `onSendMessage` callback.
   - Use controlled textarea with state.
   - Disable send button when loading or empty message.
   - Show error toast if send fails (use `sonner`).
   - Follow design tokens for input styling.

3. Connect to page: pass selected document IDs from selector to input component.

4. Verify: TypeScript compiles; hook can be called with mock data.
  </action>
  <verify>
    <automated>pnpm run type-check</automated>
  </verify>
  <done>User can type message and send to backend; hook manages API call.</done>
</task>

<task type="auto" tdd="true">
  <name>task 3: Build streaming message display with citations</name>
  <files>frontend/src/components/features/chat/chat-messages.tsx</files>
  <read_first>
    - frontend/src/lib/api-types.ts (ChatMessage, ChatChunkCitation types)
    - docs/dev/design.md (message bubble styling)
    - backend/src/api/routers/chat.py (SSE response format)
    - Any existing SSE implementation in frontend (e.g., summary streaming)
  </read_first>
  <acceptance_criteria>
    - Messages display in conversation thread
    - Streaming responses show three‑dot pulse animation with "Searching your notes..." label
    - Citations appear as inline badges or tooltips showing chunk IDs
    - Messages have user/AI differentiation (visual styling)
  </acceptance_criteria>
  <action>
1. Create `frontend/src/components/features/chat/chat-messages.tsx`:
   - Accept `messages` array prop (with `id`, `role`, `content`, `citations`).
   - Map messages to appropriate bubbles (user left, AI right).
   - For AI messages, render citations as small badges with hover tooltip showing chunk ID.
   - Implement streaming placeholder: when `isStreaming=true`, show animated three‑dot pulse with label "Searching your notes..." (D‑29).
   - Use CSS animation for dots: opacity pulse.

2. Implement SSE handling in `useSendChatMessage` hook:
   - Modify mutation to handle Server‑Sent Events.
   - Parse streamed tokens and update message content incrementally.
   - Capture citations from stream metadata if provided.

3. Style messages according to design tokens (colors, spacing, rounded corners).

4. Verify: Component renders mock messages correctly; streaming animation works.
  </action>
  <verify>
    <automated>pnpm run test:run -- --passWithNoTests</automated>
  </verify>
  <done>Messages display with citations; streaming shows proper waiting animation.</done>
</task>

<task type="auto" tdd="true">
  <name>task 4: Wire up full chat flow and add error states</name>
  <files>frontend/src/app/(app)/chat/page.tsx, frontend/src/components/features/chat/chat-input.tsx, frontend/src/components/features/chat/chat-messages.tsx</files>
  <read_first>
    - frontend/src/app/(app)/documents/page.tsx (error boundary patterns)
    - docs/dev/design.md (error state styling)
    - .planning/phases/02-core-loop-completion/02-CONTEXT.md (D‑34, D‑35)
  </read_first>
  <acceptance_criteria>
    - Chat page handles quota‑exceeded errors (display user‑friendly message)
    - Empty state shown when no documents uploaded
    - Loading skeletons during document fetch
    - Network errors display retry button
  </acceptance_criteria>
  <action>
1. Enhance chat page with error boundaries and loading states:
   - Add `Skeleton` for document loading.
   - If no documents, show empty state with link to upload page.
   - Handle API errors (quota exceeded, network) with appropriate UI messages.

2. Update `chat-input.tsx` to disable send when no documents selected.

3. Update `chat-messages.tsx` to show error messages inline (e.g., "Could not send message: quota exceeded").

4. Test error scenarios:
   - Disconnect network → show retry.
   - Quota exceeded → show upgrade prompt.
   - No documents → show empty state.

5. Ensure all UI follows design tokens and accessibility standards.

6. Final verification: `pnpm run build` and `pnpm run test:run` pass.
  </action>
  <verify>
    <automated>pnpm run build && pnpm run test:run -- --passWithNoTests</automated>
  </verify>
  <done>Full chat flow works with proper error handling and empty states.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → Chat API | User message input, document selection |
| SSE Stream → Client | Streaming AI response with citations |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-08-01 | Spoofing | Chat input | mitigate | Validate user session via Clerk; never accept userId parameter |
| T-02-08-02 | Tampering | Message content | accept | No integrity requirement; stream is read‑only |
| T-02-08-03 | Repudiation | Chat messages | accept | Not required for MVP; future audit log |
| T-02-08-04 | Information Disclosure | SSE stream | mitigate | Ensure stream endpoint requires auth; filter citations by user_id |
| T-02-08-05 | Denial of Service | Chat API calls | accept | Quota enforcement limits rate |
| T-02-08-06 | Elevation of Privilege | Document selector | mitigate | Backend filters documents by user_id; frontend only displays authorized docs |
</threat_model>

<verification>
- Navigate to `/chat` – page loads with document selector.
- Select a document, type message, click send – message appears, streaming animation shows.
- Wait for response – AI message appears with citations.
- Test error cases: no documents, quota exceeded, network error – appropriate UI shown.
</verification>

<success_criteria>
- User can chat with selected documents.
- Streaming responses display with three‑dot pulse animation and "Searching your notes..." label (D‑29).
- Citations appear as badges/tooltips.
- Error states handled gracefully.
- All TypeScript builds without errors.
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-loop-completion/02-08-SUMMARY.md`
</output>