---
phase: 02-core-loop-completion
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - backend/src/api/routers/quizzes.py
  - backend/src/services/quiz_chain.py
  - backend/src/db/models.py
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "Quiz creation enforces quota and stores generated questions in database"
    - "Quiz attempt scoring updates user_topic_performance with per-topic accuracy"
    - "Quiz chain returns questions and weak topics in format matching Quiz model"
  artifacts:
    - path: "backend/src/api/routers/quizzes.py"
      provides: "Quota enforcement on create_quiz, proper storage of chain output"
      exports: ["create_quiz", "submit_attempt"]
    - path: "backend/src/services/quiz_chain.py"
      provides: "Quiz chain returns structured questions, question_types, weak_topics"
      exports: ["run_quiz_chain"]
    - path: "backend/src/db/models.py"
      provides: "Quiz and QuizAttempt models unchanged (already exist)"
  key_links:
    - from: "backend/src/api/routers/quizzes.py"
      to: "backend/src/api/deps.py"
      via: "Depends(enforce_quota('quiz'))"
      pattern: "enforce_quota"
    - from: "backend/src/api/routers/quizzes.py"
      to: "backend/src/services/quiz_chain.py"
      via: "run_quiz_chain call"
      pattern: "run_quiz_chain"
    - from: "backend/src/api/routers/quizzes.py"
      to: "backend/src/db/models.py"
      via: "Quiz and QuizAttempt model usage"
      pattern: "Quiz"
---
<objective>
Integrate quiz chain with real database updates, implement attempt scoring and weak area detection.

Purpose: Complete the quiz generation and attempt flow with quota enforcement, database persistence, and performance tracking.

Output: Fully functional quiz creation and attempt endpoints with quota enforcement, database integration, and user_topic_performance updates.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-loop-completion/02-CONTEXT.md
@docs/dev/architecture.md
@docs/dev/master-spec.md
@backend/src/api/routers/quizzes.py
@backend/src/services/quiz_chain.py
@backend/src/db/models.py
@backend/src/api/deps.py
@backend/src/services/quota_service.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: Update quiz chain to return structured output matching Quiz model</name>
  <files>backend/src/services/quiz_chain.py</files>
  <read_first>
    - backend/src/services/quiz_chain.py (current run_quiz_chain function)
    - backend/src/db/models.py (Quiz model structure)
    - docs/dev/architecture.md (chain pattern)
    - backend/src/services/summary_chain.py (example chain output)
  </read_first>
  <acceptance_criteria>
    - run_quiz_chain returns dict with keys "questions", "question_types", "weak_topics", "weak_area_flags"
    - "questions" is a list of dicts each with "id", "question", "options", "correct_answer", "topic"
    - "question_types" is a list of strings (e.g., "multiple_choice", "true_false", "fill_in")
    - "weak_topics" is a list of topic strings derived from weak area detection
  </acceptance_criteria>
  <action>
1. Examine current `run_quiz_chain` function (lines 44-163). Identify where the chain output is assembled.
2. Modify the function to ensure the returned dict matches the expected shape for the Quiz model.
   - The Quiz model expects `questions` (JSONB) and `question_types` (list[str]).
   - The chain currently returns `questions` (list of dicts) and `question_types`.
   - Ensure each question dict includes at least: `id` (string), `question` (string), `options` (list of strings for MCQ), `correct_answer` (string), `topic` (string).
   - For true/false questions, `options` could be `["True", "False"]`.
   - For fill-in, `options` could be empty list.
   - The `weak_topics` list should be derived from weak area detection logic (if any). Currently placeholder.
   - The `weak_area_flags` dict can be left as placeholder.

3. Update the `QuizChainOutput` TypedDict to reflect the exact structure (optional but good practice).
4. Run existing tests (`pytest backend/tests/test_quiz_chain.py` if exists) to ensure no regression.
5. Run `ruff check backend/src/services/quiz_chain.py`.
  </action>
  <verify>
    <automated>ruff check backend/src/services/quiz_chain.py</automated>
  </verify>
  <done>Quiz chain returns structured data matching Quiz model expectations.</done>
</task>

<task type="auto" tdd="true">
  <name>task 2: Integrate quota enforcement and chain output storage in quizzes router</name>
  <files>backend/src/api/routers/quizzes.py</files>
  <read_first>
    - backend/src/api/routers/quizzes.py (current route implementation)
    - backend/src/api/deps.py (enforce_quota dependency)
    - backend/src/db/models.py (Quiz model)
    - backend/src/services/quiz_chain.py (run_quiz_chain signature)
    - docs/dev/architecture.md (route layer pattern)
  </read_first>
  <acceptance_criteria>
    - create_quiz route includes `Depends(enforce_quota("quiz"))`
    - After chain completes, Quiz record is updated with `questions`, `question_types`, `weak_topics`
    - Route returns appropriate response with quiz_id and weak_topics
  </acceptance_criteria>
  <action>
1. Edit `backend/src/api/routers/quizzes.py`:
   - Import `enforce_quota` from `src.api.deps`.
   - Add `_ = Depends(enforce_quota("quiz"))` as a parameter to `create_quiz` function (line ~52). Ensure it's before `user` and `db`.
   - Remove the placeholder `# Call quiz chain (stub)` comment and replace with actual chain integration:
     - After creating the pending Quiz record, call `run_quiz_chain` with the input.
     - Update the Quiz record with the chain output:
       ```python
       # Update quiz with chain output
       from sqlalchemy import update
       update_stmt = (
           update(Quiz)
           .where(Quiz.id == quiz.id)
           .values(
               questions=chain_output["questions"],
               question_types=chain_output["question_types"],
               weak_topics=chain_output["weak_topics"],
           )
       )
       await db.execute(update_stmt)
       await db.commit()
       ```
   - Ensure the response includes `weak_topics` from chain output.

2. Remove the placeholder logging lines (lines 98-105) if not needed.

3. Run `ruff check` to ensure no syntax errors.

4. Optionally test the route manually (start server, make request) to verify quota enforcement works (requires valid user token).
  </action>
  <verify>
    <automated>ruff check backend/src/api/routers/quizzes.py</automated>
  </verify>
  <done>Quiz creation enforces quota, stores chain output, returns weak topics.</done>
</task>

<task type="auto" tdd="true">
  <name>task 3: Implement quiz attempt scoring and user_topic_performance updates</name>
  <files>backend/src/api/routers/quizzes.py</files>
  <read_first>
    - backend/src/api/routers/quizzes.py (submit_attempt function)
    - backend/src/db/models.py (QuizAttempt, UserTopicPerformance models)
    - docs/dev/architecture.md (data layer patterns)
    - .planning/phases/02-core-loop-completion/02-CONTEXT.md (D-12, D-13)
  </read_first>
  <acceptance_criteria>
    - submit_attempt calculates score based on correct answers (compare user answers to stored correct_answer)
    - For each question topic, update UserTopicPerformance: increment attempts, increment correct if answer correct
    - Returns score and weak topics (topics where accuracy < 70% and attempts >= 5)
  </acceptance_criteria>
  <action>
1. Edit `submit_attempt` function in `quizzes.py`:
   - Retrieve the quiz's `questions` (list) from the Quiz record.
   - For each question ID in `request.answers`, find the corresponding question in `quiz.questions`.
   - Compare the user's selected answer with the question's `correct_answer`.
   - Count correct answers to compute score percentage.
   - For each question, determine its `topic`. If topic is empty, skip.
   - Use `UserTopicPerformance` model to update per-topic stats:
     - Use `insert(...).on_conflict_do_update(...)` to increment `attempts` and `correct` (if answer correct).
     - Example SQLAlchemy Core pattern (or use ORM). See existing patterns in codebase.
   - After updating, compute weak topics: query UserTopicPerformance for this user where `attempts >= 5 AND (correct::float / attempts) < 0.7`. Also need `quiz_count >= 2` (maybe derived from number of distinct quiz attempts per topic). For simplicity, assume `quiz_count` is number of distinct quiz IDs in QuizAttempt for that topic. Can be deferred; use simple condition for now.
   - Update the `weak_topics` variable with the list of weak topic strings.
   - Update the `QuizAttempt` record's `topics` field with weak_topics (or all topics).
   - Return response with score and weak_topics.

2. Ensure transaction safety: all updates happen within the same db session and commit at the end.

3. Remove placeholder scoring logic (lines 142-145) and placeholder weak_topics.

4. Run `ruff check` and ensure no syntax errors.

5. Write a simple test script or verify manually.
  </action>
  <verify>
    <automated>ruff check backend/src/api/routers/quizzes.py</automated>
  </verify>
  <done>Quiz attempt scoring updates user_topic_performance and returns accurate score and weak topics.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → Quiz creation | Untrusted document_id, question_count, auto_mode |
| Client → Quiz attempt | Untrusted answers mapping |
| Route → Quiz chain | Trusted internal call, but chain uses user-provided document ID |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07 | Spoofing | quiz creation | mitigate | Route uses `get_current_user` and quota enforcement; document_id must belong to user (existing check?) |
| T-02-08 | Tampering | quiz attempt answers | mitigate | Answers validated against question IDs from quiz; no injection possible |
| T-02-09 | Repudiation | scoring results | accept | No audit logs required; scores stored in QuizAttempt |
| T-02-10 | Information Disclosure | weak topics | accept | Weak topics derived from user's own performance; no cross-user leakage |
| T-02-11 | Denial of Service | quiz generation quota | mitigate | Quota enforcement limits daily requests |
| T-02-12 | Elevation of Privilege | topic performance updates | mitigate | Updates scoped to authenticated user via user_id foreign key |
</threat_model>

<verification>
1. Run `ruff check` on modified files.
2. Start FastAPI server and test quiz creation with valid token (optional).
3. Run existing tests for quiz chain and routes (`pytest backend/tests/ -v`).
</verification>

<success_criteria>
- Quiz creation enforces quota and stores generated questions.
- Quiz attempt scoring calculates accurate score and updates per-topic performance.
- Weak topics computed based on performance (simplified algorithm acceptable).
- No regression in existing functionality.
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-loop-completion/02-02-SUMMARY.md`
</output>
