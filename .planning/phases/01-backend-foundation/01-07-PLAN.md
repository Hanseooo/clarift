---
phase: 01-backend-foundation
plan: 07
type: execute
wave: 3
depends_on: [01-04, 01-05, 01-06]
files_modified:
  - backend/openapi.json
  - frontend/src/lib/api.ts
  - frontend/package.json
autonomous: true
gap_closure: true
requirements: ["1.1"]
must_haves:
  truths:
    - "Next.js has base UI and API client configured"
  artifacts:
    - path: "frontend/src/lib/api.ts"
      provides: "Real paths type from generated OpenAPI schema"
  key_links:
    - from: "frontend/src/lib/api.ts"
      to: "backend/openapi.json"
      via: "openapi-fetch paths type"
---

<objective>
Close the gap related to the missing API contract between the frontend and backend.

Purpose: Satisfy the "Next.js has base UI and API client configured" must-have by replacing the placeholder types with a generated OpenAPI schema.
Output: Generated backend OpenAPI schema and an updated frontend API client.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>task 1: Generate backend OpenAPI schema</name>
  <files>backend/openapi.json, frontend/package.json</files>
  <action>
    - Add a script to `frontend/package.json` to extract the OpenAPI schema from the running FastAPI app or to generate it statically via python script.
    - Alternatively, just write a small script in backend to dump `app.openapi()` to `backend/openapi.json`.
    - Run the script to create `backend/openapi.json`.
    
    Gap reason: Missing backend OpenAPI schema for frontend types.
  </action>
  <verify>
    <automated>test -f backend/openapi.json</automated>
  </verify>
  <done>backend/openapi.json exists and contains all routes.</done>
</task>

<task type="auto">
  <name>task 2: Generate frontend API types</name>
  <files>frontend/src/lib/api.ts, frontend/src/lib/api.d.ts</files>
  <action>
    - Run `npx openapi-typescript ../backend/openapi.json -o src/lib/api.d.ts` in the `frontend` folder (or equivalent script).
    - Update `frontend/src/lib/api.ts` to import `paths` from `./api.d.ts` instead of using the placeholder empty type.
    
    Gap reason: Placeholder paths type (`export type paths = {}`) in frontend api client.
  </action>
  <verify>
    <automated>cd frontend && pnpm tsc --noEmit</automated>
  </verify>
  <done>Frontend API client uses real paths from the backend schema and type-checks successfully.</done>
</task>

</tasks>

<verification>
Verify that the `paths` type inside `api.ts` maps correctly to the endpoints generated in the previous plans.
</verification>

<success_criteria>
- Backend OpenAPI schema is generated.
- Frontend api.ts imports real path types and compiles without error.
</success_criteria>

<output>
After completion, create `.planning/phases/01-backend-foundation/01-07-SUMMARY.md`
</output>