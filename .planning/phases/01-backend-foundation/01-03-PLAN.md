---
phase: 01-backend-foundation
plan: 03
type: execute
wave: 2
depends_on: [01, 02]
files_modified:
  - backend/src/db/models.py
  - frontend/src/db/schema.ts
  - backend/alembic/versions/
autonomous: false
requirements: [1.2, 1.3, 1.4]

must_haves:
  truths:
    - SQLAlchemy models match Drizzle schemas
    - Alembic successfully migrates the DB
  artifacts:
    - path: backend/src/db/models.py
      provides: SQLAlchemy entity mapping
    - path: frontend/src/db/schema.ts
      provides: Drizzle schemas
  key_links:
    - from: backend/src/db/models.py
      to: frontend/src/db/schema.ts
      via: exact column matching
---

<objective>
Implement database models (SQLAlchemy and Drizzle) and run initial migrations.

Purpose: Shared data contract between backend processing and frontend reads.
Output: Initialized database with users, documents, summaries, quotas, and quizzes tables.
</objective>

<context>
@docs/dev/drizzle-schema.md
</context>

<tasks>
<task type="auto">
  <name>task 1: SQLAlchemy Models</name>
  <files>backend/src/db/models.py</files>
  <action>Write SQLAlchemy models for users, user_preferences, documents, summaries, quizzes, quiz_attempts mapping EXACTLY to the structure described in `docs/dev/drizzle-schema.md`. Use `pgvector` for embedding fields if any exist now (or set up the vector type ready for the future chunks table).</action>
  <verify>
    <automated>uv run ruff check backend/src/db/models.py</automated>
  </verify>
  <done>Models pass linting and can be imported</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Alembic Revision & Database Migration</what-built>
  <how-to-verify>
    Run `cd backend && uv run alembic revision --autogenerate -m "Init models"`.
    Then run `uv run alembic upgrade head`.
    Verify in Neon DB that tables exist.
  </how-to-verify>
  <resume-signal>Type "approved" once DB is migrated.</resume-signal>
</task>

<task type="auto">
  <name>task 3: Frontend Drizzle Schema</name>
  <files>frontend/src/db/schema.ts</files>
  <action>Copy the exact schema block from `docs/dev/drizzle-schema.md` into `src/db/schema.ts`. Install `drizzle-orm` in the frontend if needed.</action>
  <verify>
    <automated>cd frontend && pnpm run lint</automated>
  </verify>
  <done>Schema matches documentation perfectly.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Backend -> DB | Model instantiation and validation |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03-1 | Elevation of Privilege | DB Migrations | mitigate | Restrict user permissions |
</threat_model>
