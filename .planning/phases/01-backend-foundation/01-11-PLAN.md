---
phase: 01-backend-foundation
plan: 11
type: execute
wave: 2
depends_on: [01-08]
files_modified:
  - frontend/src/components/upload-dropzone.tsx
  - frontend/src/app/dashboard/page.tsx
autonomous: true
gap_closure: true
requirements: [1.3]
must_haves:
  truths:
    - "User can upload a file using drag-and-drop"
    - "UI communicates with the documents upload endpoint"
  artifacts:
    - path: "frontend/src/components/upload-dropzone.tsx"
      provides: "Drag-and-drop file upload UI"
    - path: "frontend/src/app/dashboard/page.tsx"
      provides: "Dashboard view with upload"
  key_links:
    - from: "frontend/src/components/upload-dropzone.tsx"
      to: "backend/src/api/routers/documents.py"
      via: "POST /api/v1/documents/upload"
---

<objective>
Implement the frontend drag-and-drop document upload UI and integrate it with the backend pipeline.
Purpose: Closes the gap where the frontend lacked the capability to upload files to trigger the core AI loop.
Output: Working upload component in the dashboard.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/01-backend-foundation/01-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>task 1: drag and drop component</name>
  <files>frontend/src/components/upload-dropzone.tsx</files>
  <action>
    - Create a file dropzone UI with Tailwind and shadcn styling.
    - Implement file selection/drag-and-drop logic for PDFs.
    Gap reason: "Frontend drag-and-drop upload UI missing"
  </action>
  <verify>
    <automated>test -f frontend/src/components/upload-dropzone.tsx</automated>
  </verify>
  <done>Upload component compiles and accepts file objects</done>
</task>

<task type="auto">
  <name>task 2: dashboard integration</name>
  <files>frontend/src/app/dashboard/page.tsx</files>
  <action>
    - Add the dropzone to the authenticated dashboard view.
    - Wire the component to call `api.ts` POST `/api/v1/documents/upload` and initiate the SSE job stream connection.
    Gap reason: "Frontend integration to request summary after upload missing"
  </action>
  <verify>
    <automated>cd frontend && pnpm run build</automated>
  </verify>
  <done>Dashboard renders the dropzone and builds without errors</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client→API | File uploads |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-11-01 | Tampering | Dropzone | mitigate | Enforce MIME type constraints on client, but expect server-side validation |
</threat_model>

<verification>
`cd frontend && pnpm run build` verifies UI compilation.
</verification>

<success_criteria>
The frontend dashboard allows the user to drag-and-drop files, successfully triggering backend processing.
</success_criteria>

<output>
After completion, create `.planning/phases/01-backend-foundation/01-11-SUMMARY.md`
</output>
