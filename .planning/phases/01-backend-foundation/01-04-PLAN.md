---
phase: 01-backend-foundation
plan: 04
type: execute
wave: 2
depends_on: [01-01, 01-02, 01-03]
files_modified:
  - backend/src/api/deps.py
  - backend/src/api/routers/auth.py
  - backend/src/api/routers/jobs.py
  - backend/main.py
autonomous: true
gap_closure: true
requirements: ["1.2"]
must_haves:
  truths:
    - "Auth endpoints exist and are wired"
    - "Jobs endpoints exist and are wired"
  artifacts:
    - path: "backend/src/api/routers/auth.py"
      provides: "Auth router with POST /sync and GET /me"
    - path: "backend/src/api/routers/jobs.py"
      provides: "Jobs router with SSE streaming"
  key_links:
    - from: "backend/main.py"
      to: "backend/src/api/routers/auth.py"
      via: "app.include_router"
---

<objective>
Close gaps related to missing Auth and Jobs endpoints identified in phase 1 verification.

Purpose: Satisfy the "All endpoints exist and are wired" must-have truth for Auth and Jobs.
Output: Auth router, Jobs router, and updated main.py with included routers.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>task 1: Implement auth router and dependencies</name>
  <files>backend/src/api/deps.py, backend/src/api/routers/auth.py</files>
  <action>
    - Create JWT validation dependency in `backend/src/api/deps.py`.
    - Implement auth router in `backend/src/api/routers/auth.py` with `POST /sync` and `GET /me` endpoints.
    
    Gap reason: No routers included in backend/main.py; missing auth endpoints.
  </action>
  <verify>
    <automated>uv run pytest tests -k "auth" || echo "MISSING — Wave 0 must create test_auth.py first"</automated>
  </verify>
  <done>Auth router has POST /sync and GET /me endpoints.</done>
</task>

<task type="auto">
  <name>task 2: Implement jobs router</name>
  <files>backend/src/api/routers/jobs.py</files>
  <action>
    - Implement jobs router in `backend/src/api/routers/jobs.py` for streaming progress via SSE.
    
    Gap reason: Missing jobs endpoints for streaming progress.
  </action>
  <verify>
    <automated>uv run pytest tests -k "jobs" || echo "MISSING — Wave 0 must create test_jobs.py first"</automated>
  </verify>
  <done>Jobs router has SSE streaming endpoint.</done>
</task>

<task type="auto">
  <name>task 3: Wire routers in main.py</name>
  <files>backend/main.py</files>
  <action>
    - Import and include `auth_router` and `jobs_router` in `backend/main.py` using `app.include_router`.
    
    Gap reason: TODO placeholder for routers, no routers actually included.
  </action>
  <verify>
    <automated>uv run pytest tests -k "main" || echo "MISSING — Wave 0 must create test_main.py first"</automated>
  </verify>
  <done>Auth and jobs routers are included in the FastAPI app instance.</done>
</task>

</tasks>

<verification>
Ensure the FastAPI app boots successfully without import errors and exposes the new routes in the OpenAPI schema.
</verification>

<success_criteria>
- Auth router exists and is wired in main.py.
- Jobs router exists and is wired in main.py.
</success_criteria>

<output>
After completion, create `.planning/phases/01-backend-foundation/01-04-SUMMARY.md`
</output>