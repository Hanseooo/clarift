---
phase: 01-backend-foundation
plan: 09
type: execute
wave: 1
depends_on: [01-05]
files_modified:
  - backend/src/services/quota_service.py
  - backend/src/api/routers/documents.py
autonomous: true
gap_closure: true
requirements: [1.3, 1.4]
must_haves:
  truths:
    - "Quota service logic limits API access"
    - "Document upload dispatches an ARQ job"
  artifacts:
    - path: "backend/src/services/quota_service.py"
      provides: "Quota checking functions"
    - path: "backend/src/api/routers/documents.py"
      provides: "ARQ job creation"
  key_links:
    - from: "backend/src/api/routers/documents.py"
      to: "backend/src/worker.py"
      via: "arq_pool.enqueue_job"
---

<objective>
Implement quota enforcement logic and replace the ARQ job dispatch stub in the documents upload endpoint.
Purpose: Closes gaps in quota enforcement and document processing initialization.
Output: Working backend quota service and real ARQ task creation.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/01-backend-foundation/01-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>task 1: quota service implementation</name>
  <files>backend/src/services/quota_service.py</files>
  <action>
    - Create a basic quota check and increment logic per user.
    - Implement functions to check if the user has available quota, and debit it.
    Gap reason: "Quota service missing"
  </action>
  <verify>
    <automated>test -f backend/src/services/quota_service.py</automated>
  </verify>
  <done>quota service functions exist</done>
</task>

<task type="auto">
  <name>task 2: ARQ job dispatch</name>
  <files>backend/src/api/routers/documents.py</files>
  <action>
    - Replace the placeholder "TODO: Dispatch ARQ job (stub)" with actual `arq_pool.enqueue_job('process_document', document_id)`.
    - Use the arq Redis connection pool.
    Reference existing code: documents router logic for job record creation.
    Gap reason: "ARQ job dispatch not implemented"
  </action>
  <verify>
    <automated>grep enqueue_job backend/src/api/routers/documents.py</automated>
  </verify>
  <done>ARQ job is actively enqueued upon upload</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API→Quota | Internal quota checks before resource heavy tasks |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-09-01 | DoS | Quota Service | mitigate | Enforce daily processing caps |
</threat_model>

<verification>
Ensure `enqueue_job` is used in `documents.py` and `quota_service.py` functions type check.
</verification>

<success_criteria>
Quota service code exists and the ARQ job logic is fully wired.
</success_criteria>

<output>
After completion, create `.planning/phases/01-backend-foundation/01-09-SUMMARY.md`
</output>
