---
phase: 01-backend-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/pyproject.toml
  - backend/main.py
  - backend/src/core/config.py
  - backend/src/core/exceptions.py
  - backend/src/db/session.py
  - backend/alembic.ini
  - backend/alembic/env.py
autonomous: true
requirements: [1.1]

must_haves:
  truths:
    - FastAPI app runs locally with healthcheck
    - SQLAlchemy async session is configured
    - Alembic is initialized for Neon DB
  artifacts:
    - path: backend/main.py
      provides: FastAPI application instance
    - path: backend/alembic.ini
      provides: Migration configuration
  key_links:
    - from: backend/main.py
      to: backend/src/core/config.py
      via: settings import
---

<objective>
Initialize the FastAPI application structure, core configuration, error handling, and database connection.

Purpose: Establishes the backend skeleton required for all subsequent API endpoints and models.
Output: Running FastAPI app, SQLAlchemy session setup, and Alembic initialization.
</objective>

<context>
@docs/dev/stack-setup.md
@docs/dev/architecture.md
</context>

<tasks>
<task type="auto">
  <name>task 1: Core FastAPI App & Config</name>
  <files>backend/main.py, backend/src/core/config.py, backend/src/core/exceptions.py</files>
  <action>Create a robust FastAPI app in `main.py` replacing the stub. Add CORS middleware, global exception handlers (e.g., generic 500, validation errors), and Sentry init (if DSN provided). Create `config.py` using Pydantic `BaseSettings` for env vars (DATABASE_URL, REDIS_URL, R2_*, JWT_SECRET, GEMINI_API_KEY).</action>
  <verify>
    <automated>uv run uvicorn main:app --reload --host 127.0.0.1 --port 8000 & sleep 2 && curl -s http://127.0.0.1:8000/health || true</automated>
  </verify>
  <done>App starts and responds to /health</done>
</task>

<task type="auto">
  <name>task 2: Database & Alembic Setup</name>
  <files>backend/src/db/session.py, backend/alembic.ini, backend/alembic/env.py</files>
  <action>Configure SQLAlchemy `async_sessionmaker` in `session.py` using Neon PostgreSQL (`DATABASE_URL`). Initialize Alembic (`uv run alembic init alembic`). Modify `alembic/env.py` to support async engine and import Base metadata for autogenerate.</action>
  <verify>
    <automated>uv run alembic current</automated>
  </verify>
  <done>Alembic is configured and can connect to DB</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| API -> DB | SQL injection risk at SQLAlchemy layer |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01-1 | Tampering | DB Session | mitigate | Use SQLAlchemy ORM param binding |
</threat_model>
