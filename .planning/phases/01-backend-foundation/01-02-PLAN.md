---
phase: 01-backend-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/src/lib/api.ts
  - frontend/src/lib/utils.ts
autonomous: true
requirements: [1.1, 1.2]

must_haves:
  truths:
    - Next.js has base UI and API client configured
  artifacts:
    - path: frontend/src/lib/api.ts
      provides: openapi-fetch client
  key_links:
    - from: frontend/src/lib/api.ts
      to: backend/openapi.json
      via: fetch
---

<objective>
Initialize the Next.js frontend, install UI tools, and setup the API client.

Purpose: Creates the foundation for the frontend development and API communication.
Output: Working Next.js app with `openapi-fetch`
</objective>

<context>
@docs/dev/architecture.md
</context>

<tasks>
<task type="auto">
  <name>task 1: Next.js & UI Base Setup</name>
  <files>frontend/package.json, frontend/src/lib/utils.ts</files>
  <action>Add essential shadcn/ui dependencies (clsx, tailwind-merge, lucide-react) and create utility functions (`cn` in `src/lib/utils.ts`). Ensure Tailwind 4 configuration is correct for `src/` directory.</action>
  <verify>
    <automated>cd frontend && pnpm run build</automated>
  </verify>
  <done>Frontend builds successfully</done>
</task>

<task type="auto">
  <name>task 2: API Client Setup</name>
  <files>frontend/src/lib/api.ts, frontend/package.json</files>
  <action>Install `openapi-fetch`. Create `api.ts` with `createClient<paths>({ baseUrl: process.env.NEXT_PUBLIC_API_URL })`. Add a dummy or imported `paths` type temporarily until the backend schema is generated.</action>
  <verify>
    <automated>cd frontend && pnpm tsc --noEmit</automated>
  </verify>
  <done>TypeScript type-checks successfully</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Client -> Next.js | Input rendering in browser |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02-1 | Spoofing | API Client | mitigate | Use strict typed payloads via openapi |
</threat_model>
