---
phase: 01-backend-foundation
plan: 10
type: execute
wave: 2
depends_on: [01-09]
files_modified:
  - backend/src/worker.py
  - backend/src/services/summary_chain.py
  - backend/src/services/quiz_chain.py
  - backend/src/services/chat_chain.py
autonomous: true
gap_closure: true
requirements: [1.3, 1.4]
must_haves:
  truths:
    - "AI chains invoke LLM rather than stubs"
    - "ARQ worker processes documents"
  artifacts:
    - path: "backend/src/worker.py"
      provides: "Real ARQ job worker"
    - path: "backend/src/services/summary_chain.py"
      provides: "LangChain/LLM calls"
  key_links:
    - from: "backend/src/services/summary_chain.py"
      to: "Gemini API"
      via: "LangChain/langgraph"
---

<objective>
Implement the actual LLM integration for the core AI chains (summary, quiz, chat) and complete the ARQ worker pipeline.
Purpose: Closes gaps related to stub AI models preventing actual document processing and analysis.
Output: Core loop runs end-to-end utilizing Gemini/LLM instead of hardcoded stubs.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/01-backend-foundation/01-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>task 1: real ARQ worker</name>
  <files>backend/src/worker.py</files>
  <action>
    - Remove placeholder implementation.
    - Wire `process_document` to fetch the file, extract text, chunk it, and update the database state.
    Gap reason: "Real ARQ worker processing documents missing"
  </action>
  <verify>
    <automated>grep -v "TODO" backend/src/worker.py | grep "process_document"</automated>
  </verify>
  <done>ARQ worker correctly executes background text extraction</done>
</task>

<task type="auto">
  <name>task 2: implement AI chains</name>
  <files>backend/src/services/summary_chain.py, backend/src/services/quiz_chain.py, backend/src/services/chat_chain.py</files>
  <action>
    - Replace the stub returns with actual LangChain calls using the `ChatGoogleGenerativeAI` wrapper (Gemini).
    - Ensure typed output validation is functioning.
    Gap reason: "LLM integration for summary, quiz, and chat stubs missing"
  </action>
  <verify>
    <automated>grep "ChatGoogleGenerativeAI" backend/src/services/summary_chain.py</automated>
  </verify>
  <done>AI chains are backed by Gemini LLM instances</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AI→DB | Extracted text and outputs are safely stored |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-10-01 | Injection | Prompt injection | mitigate | Filter LLM inputs |
</threat_model>

<verification>
Ensure Gemini model instances replace hardcoded values in `summary_chain.py`, `quiz_chain.py`, and `chat_chain.py`.
</verification>

<success_criteria>
The backend core loop performs real generations via Gemini and processes files via ARQ.
</success_criteria>

<output>
After completion, create `.planning/phases/01-backend-foundation/01-10-SUMMARY.md`
</output>
