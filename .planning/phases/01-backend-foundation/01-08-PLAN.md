---
phase: 01-backend-foundation
plan: 08
type: execute
wave: 1
depends_on: [01-04]
files_modified:
  - frontend/package.json
  - frontend/src/auth.ts
  - frontend/src/app/api/auth/[...nextauth]/route.ts
  - frontend/src/app/login/page.tsx
  - frontend/src/components/login-button.tsx
autonomous: true
gap_closure: true
requirements: [1.2]
must_haves:
  truths:
    - "User can sign in with Google"
    - "Session syncs to backend"
  artifacts:
    - path: "frontend/src/auth.ts"
      provides: "NextAuth v5 configuration"
    - path: "frontend/src/app/login/page.tsx"
      provides: "Login UI"
  key_links:
    - from: "frontend/src/auth.ts"
      to: "backend/src/api/routers/auth.py"
      via: "NextAuth callbacks / POST /api/v1/auth/sync"
---

<objective>
Implement frontend authentication UI using NextAuth v5 and Google OAuth.
Purpose: Closes verification gap where requirement 1.2 was only partially satisfied because the backend endpoints existed but the frontend NextAuth configuration and login UI were missing.
Output: Configured NextAuth v5, Google OAuth provider, and functional login page.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/01-backend-foundation/01-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>task 1: install next-auth</name>
  <files>frontend/package.json</files>
  <action>
    - Install `next-auth@beta` for NextAuth v5 in the frontend directory.
    Gap reason: "Frontend auth UI (NextAuth v5) not implemented"
  </action>
  <verify>
    <automated>cd frontend && pnpm ls next-auth</automated>
  </verify>
  <done>next-auth is in package dependencies</done>
</task>

<task type="auto">
  <name>task 2: configure nextauth</name>
  <files>frontend/src/auth.ts, frontend/src/app/api/auth/[...nextauth]/route.ts</files>
  <action>
    - Create NextAuth v5 configuration with Google OAuth provider.
    - Implement the `signIn` or `jwt` callback to call the backend `POST /api/v1/auth/sync` endpoint with the Google access token to sync the user.
    Reference existing code: backend auth router already implemented in plan 01-04.
    Gap reason: "NextAuth v5 setup with Google OAuth missing"
  </action>
  <verify>
    <automated>test -f frontend/src/auth.ts</automated>
  </verify>
  <done>NextAuth configuration exports handlers and auth object</done>
</task>

<task type="auto">
  <name>task 3: build login UI</name>
  <files>frontend/src/app/login/page.tsx, frontend/src/components/login-button.tsx</files>
  <action>
    - Create a clean login page component.
    - Create a login button that triggers NextAuth `signIn("google")`.
    Gap reason: "Login page and session sync missing"
  </action>
  <verify>
    <automated>cd frontend && pnpm run build</automated>
  </verify>
  <done>Login page builds successfully</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser→NextAuth | OAuth token exchange |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-08-01 | Spoofing | NextAuth | mitigate | Require secure environment variables for Google Client ID/Secret |
</threat_model>

<verification>
Run `cd frontend && pnpm run build` to verify NextAuth compilation.
</verification>

<success_criteria>
NextAuth v5 is fully configured with a Google provider and a login page exists.
</success_criteria>

<output>
After completion, create `.planning/phases/01-backend-foundation/01-08-SUMMARY.md`
</output>
