---
phase: 01-backend-foundation
plan: 06
type: execute
wave: 2
depends_on: [01-01, 01-02, 01-03]
files_modified:
  - backend/src/services/quiz_chain.py
  - backend/src/api/routers/quizzes.py
  - backend/src/api/routers/practice.py
  - backend/src/api/routers/chat.py
  - backend/main.py
autonomous: true
gap_closure: true
requirements: ["1.4"]
must_haves:
  truths:
    - "Quizzes, Practice, and Chat endpoints exist and are wired"
  artifacts:
    - path: "backend/src/services/quiz_chain.py"
      provides: "Quiz generation with weak area detection"
    - path: "backend/src/api/routers/quizzes.py"
      provides: "Quizzes router"
    - path: "backend/src/api/routers/chat.py"
      provides: "Chat router"
  key_links:
    - from: "backend/main.py"
      to: "backend/src/api/routers/quizzes.py"
      via: "app.include_router"
---

<objective>
Close gaps related to missing Quizzes, Practice, and Chat endpoints identified in phase 1 verification.

Purpose: Satisfy the "All endpoints exist and are wired" and "Core loop works end-to-end" must-haves for Quizzes and Chat.
Output: Quiz generation logic and updated main.py with included routers.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>task 1: Implement quiz chain</name>
  <files>backend/src/services/quiz_chain.py, backend/src/api/routers/quizzes.py</files>
  <action>
    - Implement quiz generation with weak area detection in `backend/src/services/quiz_chain.py`.
    - Implement quizzes router (`POST /quizzes`, `POST /quizzes/attempts`) in `backend/src/api/routers/quizzes.py`.
    
    Gap reason: Missing quiz generation logic and router.
  </action>
  <verify>
    <automated>uv run pytest tests -k "quizzes" || echo "MISSING — Wave 0 must create test_quizzes.py first"</automated>
  </verify>
  <done>Quiz chain service exists and quizzes router exposes it.</done>
</task>

<task type="auto">
  <name>task 2: Implement practice and chat routers</name>
  <files>backend/src/api/routers/practice.py, backend/src/api/routers/chat.py</files>
  <action>
    - Implement targeted drills practice router in `backend/src/api/routers/practice.py`.
    - Implement grounded chat router in `backend/src/api/routers/chat.py`.
    
    Gap reason: Missing practice and chat endpoints.
  </action>
  <verify>
    <automated>uv run pytest tests -k "chat_or_practice" || echo "MISSING — Wave 0 must create tests first"</automated>
  </verify>
  <done>Practice and chat routers expose functional stub endpoints.</done>
</task>

<task type="auto">
  <name>task 3: Wire routers in main.py</name>
  <files>backend/main.py</files>
  <action>
    - Import and include `quizzes_router`, `practice_router`, and `chat_router` in `backend/main.py` using `app.include_router`.
    
    Gap reason: TODO placeholder for routers, no routers actually included.
  </action>
  <verify>
    <automated>uv run pytest tests -k "main" || echo "MISSING — Wave 0 must create test_main.py first"</automated>
  </verify>
  <done>Quizzes, practice, and chat routers are included in the FastAPI app instance.</done>
</task>

</tasks>

<verification>
Ensure the FastAPI app boots successfully without import errors and exposes the new routes in the OpenAPI schema.
</verification>

<success_criteria>
- Quizzes router exists and is wired in main.py.
- Practice router exists and is wired in main.py.
- Chat router exists and is wired in main.py.
</success_criteria>

<output>
After completion, create `.planning/phases/01-backend-foundation/01-06-SUMMARY.md`
</output>