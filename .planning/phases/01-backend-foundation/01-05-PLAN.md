---
phase: 01-backend-foundation
plan: 05
type: execute
wave: 2
depends_on: [01-01, 01-02, 01-03]
files_modified:
  - backend/src/api/routers/documents.py
  - backend/src/worker.py
  - backend/src/services/summary_chain.py
  - backend/src/api/routers/summaries.py
  - backend/main.py
autonomous: true
gap_closure: true
requirements: ["1.3", "1.4"]
must_haves:
  truths:
    - "Document pipeline exists and is wired"
    - "Summary chain exists and is wired"
  artifacts:
    - path: "backend/src/api/routers/documents.py"
      provides: "Documents router with upload endpoint"
    - path: "backend/src/services/summary_chain.py"
      provides: "Summary chain (5-step)"
    - path: "backend/src/api/routers/summaries.py"
      provides: "Summaries router"
  key_links:
    - from: "backend/main.py"
      to: "backend/src/api/routers/documents.py"
      via: "app.include_router"
---

<objective>
Close gaps related to missing Documents and Summaries endpoints identified in phase 1 verification.

Purpose: Satisfy the "Core loop works end-to-end" and "All endpoints exist and are wired" must-haves for Document upload and Summary chains.
Output: Document processing pipeline stub, Summary chain implementation, and updated main.py with included routers.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>task 1: Implement documents router</name>
  <files>backend/src/api/routers/documents.py, backend/src/worker.py</files>
  <action>
    - Implement document upload endpoint (`POST /documents/upload`) and ARQ job processing stub.
    
    Gap reason: Missing document upload pipeline and ARQ job processing.
  </action>
  <verify>
    <automated>uv run pytest tests -k "documents" || echo "MISSING — Wave 0 must create test_documents.py first"</automated>
  </verify>
  <done>Documents router has an upload endpoint handling ARQ job dispatch.</done>
</task>

<task type="auto">
  <name>task 2: Implement summary chain</name>
  <files>backend/src/services/summary_chain.py, backend/src/api/routers/summaries.py</files>
  <action>
    - Implement summary chain (5-step) implementation in `backend/src/services/summary_chain.py`.
    - Implement summaries router (`POST /summaries` or equivalent) in `backend/src/api/routers/summaries.py`.
    
    Gap reason: Missing summary chain implementation and summaries router.
  </action>
  <verify>
    <automated>uv run pytest tests -k "summaries" || echo "MISSING — Wave 0 must create test_summaries.py first"</automated>
  </verify>
  <done>Summary chain service exists and summaries router exposes it.</done>
</task>

<task type="auto">
  <name>task 3: Wire routers in main.py</name>
  <files>backend/main.py</files>
  <action>
    - Import and include `documents_router` and `summaries_router` in `backend/main.py` using `app.include_router`.
    
    Gap reason: TODO placeholder for routers, no routers actually included.
  </action>
  <verify>
    <automated>uv run pytest tests -k "main" || echo "MISSING — Wave 0 must create test_main.py first"</automated>
  </verify>
  <done>Documents and summaries routers are included in the FastAPI app instance.</done>
</task>

</tasks>

<verification>
Ensure the FastAPI app boots successfully without import errors and exposes the new routes in the OpenAPI schema.
</verification>

<success_criteria>
- Documents router exists and is wired in main.py.
- Summaries router exists and is wired in main.py.
</success_criteria>

<output>
After completion, create `.planning/phases/01-backend-foundation/01-05-SUMMARY.md`
</output>